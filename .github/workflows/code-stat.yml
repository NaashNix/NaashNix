name: Waka Readme

on:
  schedule:
    # Runs at 12am IST
    - cron: '30 18 * * *'
  workflow_dispatch:
jobs:
  update-readme:
    name: Update Readme with Metrics
    runs-on: ubuntu-latest
    steps:
      - uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.853baa12-a3a4-43ec-b16f-f2b7f9187cf9 }}
          GH_TOKEN: ${{ secrets.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 }}
